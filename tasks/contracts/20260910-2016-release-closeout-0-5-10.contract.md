# Task Contract: release-closeout-0-5-10

> **Status**: Active
> **Plan**: plans/plan-20260910-2016-release-closeout-0-5-10.md
> **Task Profile**: code-change
> <!-- legal values: code-change | docs-only | ledger-closeout | migration | eval-only | delegated-run | bugfix (omit for legacy passthrough); see docs/reference-configs/sprint-contracts.md -->
> **Owner**: ancienttwo
> **Capability ID**: root
> **Last Updated**: 2026-09-10 20:16
> **Review File**: `tasks/reviews/20260910-2016-release-closeout-0-5-10.review.md`
> **Notes File**: `tasks/notes/20260910-2016-release-closeout-0-5-10.notes.md`
> **Exemplar**: `docs/reference-configs/contract-brief-example.md`

## Why

The release record is blocked by a schema-1 sprint and redundant fixture startup work that exceeded Windows CI test deadlines.

## Goal

Preserve the old sprint task identities through the official migration and retain both CLI cases with one runtime lifecycle each, then merge the verified publication record.

## Scope

- In scope: two CLI test fixtures, official sprint migration receipt, publication records and workflow artifacts.
- Out of scope: production behavior, published package payloads, timeout changes, shared runtime restart and unrelated historical acceptance.

## Stop Conditions

- Stop and hand back to the parent if the change would require editing a path outside Allowed Paths.
- Stop if an Exit Criteria command cannot be run in this environment.
- Stop if Goal, Scope, or Exit Criteria are internally contradictory.

## Falsifier

Any changed protocol or lockfile assertion, altered task status or identity, or failing exact-head Windows CI prevents closure.

## Root Cause Evidence

- root_cause: runTestCli in packages/surfaces/cli/test/cli.test.ts creates five embedded daemon/SQLite lifecycles in each affected case, plus unused Git history setup.
- repro: Required CI 34467186132 jobs 102838398465 and 102838398275 timed out in these existing fixtures; the projection failure is at git commit before product execution.
- regression_guard: packages/surfaces/cli/test/cli.test.ts, the two named cases retain all 26 assertions and their 15000 ms budget.
- pre_fix_failure_artifact: https://github.com/Ancienttwo/arch-context/actions/runs/34467186132; local baseline passes and does not claim to reproduce Windows timing.

## Workflow Inventory

- Source plan: `plans/plan-20260910-2016-release-closeout-0-5-10.md`
- Deferred-goal ledger: `tasks/todos.md`
- Review file: `tasks/reviews/20260910-2016-release-closeout-0-5-10.review.md`
- Notes file: `tasks/notes/20260910-2016-release-closeout-0-5-10.notes.md`
- Checks file: `.ai/harness/checks/latest.json`
- Run snapshots: `.ai/harness/runs/`
- Scope gate: edit only paths listed under `allowed_paths`; update this contract before widening scope.
- Completion gate: run `verify-sprint --prepare-acceptance`, record one typed AcceptanceReceipt under the frozen policy below, then run `verify-sprint`; review Markdown is projection only.

## Change Assessment

```json
{"protocol": 1, "oracles": [{"id": "cli-fixture-semantics", "kind": "deterministic_test", "paths": ["*"]}, {"id": "sprint-migration-receipt", "kind": "runtime_readback", "paths": ["*"]}]}
```

## Acceptance Policy

```json
{"protocol":2,"reviewer":"Codex","source":"codex-plugin","user_waiver":"allowed"}
```

## Allowed Paths

```yaml
allowed_paths:
  - packages/surfaces/cli/test/cli.test.ts
  - plans/
  - tasks/
  - docs/runbooks/personal-user-install.md
  - docs/spec.md
  - docs/verification/archctx-0.5.10-release.json
```

## Evidence Requirements

```yaml
evidence_requirements:
  # Set benchmark to required when this contract consumes the harness profile benchmark matrix.
  benchmark: not_applicable
```

## Delegation Contract

```yaml
delegation:
  budget:
    tokens: null
    runner_invocations: null
    wall_time_minutes: null
  permission_scope:
    mode: inherit_allowed_paths
    writable_paths: []
    network: inherited
  roles:
    parent:
      mode: narrate_and_gatekeep
      purpose: approval_checkpoint_owner
    explorer:
      mode: read_only
      purpose: codebase_research
    worker:
      mode: edit_within_allowed_paths
      purpose: implementation
    verifier:
      mode: read_only
      purpose: exit_criteria_review
  runner:
    preferred:
      - subagent
    fallback: null
    brief_is_authoritative: true
```

## Exit Criteria (Machine Verifiable)

```yaml
exit_criteria:
  files_exist:
    - plans/sprints/20260902-2336-refactor-instrumentation-resolution-ledger.schema-migration.v1.json
    - docs/verification/archctx-0.5.10-release.json
```

## Verification Plan

```json
{
  "protocol": 1,
  "checks": [
    {
      "id": "cli-fixtures",
      "kind": "command",
      "command": "bun test packages/surfaces/cli/test/cli.test.ts --test-name-pattern 'CLI projection run consumes|CLI docs commands keep Context7'",
      "cwd": ".",
      "phase": "verification",
      "cost": "normal",
      "evidence_policy": "current_exact",
      "necessity": "Preserve the actual projection envelope, stale-fence rejection and explicit docs lockfile/egress behavior with all 26 existing assertions.",
      "inputs": {
        "env": [
          "PATH"
        ]
      }
    },
    {
      "id": "typecheck",
      "kind": "command",
      "command": "bun run typecheck",
      "cwd": ".",
      "phase": "verification",
      "cost": "normal",
      "evidence_policy": "current_exact",
      "necessity": "Validate the supported RuntimeDaemonClient injection and test store contract.",
      "inputs": {
        "env": [
          "PATH"
        ]
      }
    },
    {
      "id": "workflow-closeout",
      "kind": "command",
      "command": "repo-harness run check-deploy-sql-order && repo-harness run check-architecture-sync && repo-harness run check-task-sync && repo-harness run check-task-workflow --strict && repo-harness run inspect-project-state --repo . --format text && repo-harness init --repo . --dry-run",
      "cwd": ".",
      "phase": "verification",
      "cost": "normal",
      "evidence_policy": "current_exact",
      "necessity": "Validate strict migrated workflow and unchanged downstream adoption integrity without repeating the full published-source governance baseline.",
      "inputs": {
        "env": [
          "PATH"
        ]
      }
    }
  ]
}
```

## Acceptance Notes (Human Review)

- Fixture optimization changes no production source. Dedicated SQLite integration and daemon discovery tests retain their responsibility.
- Local baseline: 2 pass, 26 assertions, 2.02 seconds. After change: 2 pass, 26 assertions, 1.306 seconds. These single samples are not a Windows performance claim.
- Required CI must pass on the final PR head; the previous Windows failures remain recorded.
- The migration preserves all 10 task IDs and statuses. All seven completing leases were canonically confirmed completed before reconciliation.

## Rollback Point

- Commit / checkpoint: d86fc48492e6cd0934c04bce8ed20186cc5d052e.
- Revert only this bounded fixture/workflow delta; do not recreate terminal leases or overwrite published artifacts.
