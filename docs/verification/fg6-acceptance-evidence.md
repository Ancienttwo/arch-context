# FG6 Acceptance Evidence

- Environment: local checkout `/Users/chris/Projects/arch-context`
- Scope: AC-01 through AC-06 release evidence manifest plus adversarial governance, chaos/fault, security release, retention/deletion, ops runbook, feature flag, rollback compatibility, and FG6-EG1 through FG6-EG8 closure
- Started At: 2026-06-22

| Acceptance | Sprint task | Status | Evidence | Verification |
|---|---|---|---|---|
| AC-01 Local user without GitHub App | FG6-02 | Verified | `docs/verification/fg6-local-no-cloud-readback.json`; `scripts/fg6-local-no-cloud-readback.ts`; `scripts/local-no-cloud-e2e.mjs` | `bun run readback:fg6:local-no-cloud`; `bun scripts/fg6-local-no-cloud-readback.ts inspect --evidence docs/verification/fg6-local-no-cloud-readback.json --json` |
| AC-02 Developer Review | FG6-03 | Verified | `docs/verification/fg6-developer-review-provenance-readback.json`; `docs/verification/fg3-developer-review-check-readback.json`; `docs/verification/fg3-developer-review-process-e2e.json`; `scripts/fg6-developer-review-provenance-readback.ts` | `bun run readback:fg6:developer-review-provenance`; `bun scripts/fg6-developer-review-provenance-readback.ts inspect --evidence docs/verification/fg6-developer-review-provenance-readback.json --json` |
| AC-03 New Commit invalidation | FG6-04 | Verified | `docs/verification/fg6-new-commit-invalidation-readback.json`; `docs/verification/fg3-check-supersede-readback.json`; `docs/verification/fg3-real-pr-synchronize-e2e.json`; `scripts/fg6-new-commit-invalidation-readback.ts` | `bun run readback:fg6:new-commit-invalidation`; `bun scripts/fg6-new-commit-invalidation-readback.ts inspect --evidence docs/verification/fg6-new-commit-invalidation-readback.json --json` |
| AC-04 Organization Runner no LLM | FG6-05 | Verified | `docs/verification/fg6-organization-runner-no-llm-readback.json`; `docs/verification/fg4-github-hosted-runner-readback.json`; `docs/verification/fg4-organization-runner-ruleset-readback.json`; `docs/verification/fg4-deterministic-conclusion-readback.json`; `scripts/fg6-organization-runner-no-llm-readback.ts` | `bun run readback:fg6:organization-runner-no-llm`; `bun scripts/fg6-organization-runner-no-llm-readback.ts inspect --evidence docs/verification/fg6-organization-runner-no-llm-readback.json --json` |
| AC-05 Privacy DLP | FG6-06 | Verified | `docs/verification/fg6-privacy-dlp-readback.json`; `docs/verification/fg3-cloud-dlp-readback.json`; `docs/verification/fg4-runner-dlp-readback.json`; `docs/verification/fg5-full-plane-dlp-readback.json`; `scripts/fg6-privacy-dlp-readback.ts` | `bun run readback:fg6:privacy-dlp`; `bun scripts/fg6-privacy-dlp-readback.ts inspect --evidence docs/verification/fg6-privacy-dlp-readback.json --json` |
| AC-06 No LLM deterministic gates | FG6-07 | Verified | `docs/verification/fg6-no-provider-deterministic-readback.json`; `docs/verification/fg6-local-no-cloud-readback.json`; `docs/verification/fg4-github-hosted-runner-readback.json`; `docs/verification/fg4-deterministic-conclusion-readback.json`; `docs/verification/fg6-organization-runner-no-llm-readback.json`; `scripts/fg6-no-provider-deterministic-readback.ts` | `bun run readback:fg6:no-provider-deterministic`; `bun scripts/fg6-no-provider-deterministic-readback.ts inspect --evidence docs/verification/fg6-no-provider-deterministic-readback.json --json` |
| Adversarial governance matrix | FG6-09 | Verified | `docs/verification/fg6-adversarial-governance-matrix-readback.json`; `docs/verification/fg2-staging-evidence.json`; `docs/verification/fg4-public-fork-adversarial-readback.json`; `docs/verification/fg2-install-revoke-readback.json`; `docs/verification/fg4-organization-runner-ruleset-readback.json`; `docs/verification/fg4-developer-cannot-satisfy-organization-readback.json`; `docs/verification/fg4-runner-key-lifecycle-e2e.json`; `scripts/fg6-adversarial-governance-matrix-readback.ts` | `bun run readback:fg6:adversarial-governance-matrix`; `bun scripts/fg6-adversarial-governance-matrix-readback.ts inspect --evidence docs/verification/fg6-adversarial-governance-matrix-readback.json --json` |
| Chaos/fault matrix | FG6-10 | Verified | `docs/verification/fg6-chaos-fault-matrix-readback.json`; `docs/verification/fg5-check-failure-readback.json`; `docs/verification/fg5-control-plane-incident-drill.json`; `docs/verification/fg5-control-plane-gate.md`; `packages/cloud/control-plane/test/control-plane.test.ts`; `scripts/fg6-chaos-fault-matrix-readback.ts` | `bun run readback:fg6:chaos-fault-matrix`; `bun scripts/fg6-chaos-fault-matrix-readback.ts inspect --evidence docs/verification/fg6-chaos-fault-matrix-readback.json --json` |
| Security release scan | FG6-11 | Verified | `docs/verification/fg6-security-release-readback.json`; `docs/security/reviews/fg6-release-security-scan.md`; `docs/security/scans/fg6-release-sbom.cdx.json`; `docs/security/scans/manifest.json`; `scripts/fg6-security-release-readback.ts` | `bun run readback:fg6:security-release`; `bun scripts/fg6-security-release-readback.ts inspect --evidence docs/verification/fg6-security-release-readback.json --json` |
| External security review | FG6-12 | Verified | `docs/verification/fg6-external-security-review-readback.json`; `docs/security/reviews/fg6-external-security-review.md`; `docs/verification/fg6-privacy-dlp-readback.json`; `docs/verification/fg6-adversarial-governance-matrix-readback.json`; `docs/verification/fg6-security-release-readback.json`; `docs/verification/fg3-attestation-security-suite.json`; `scripts/fg6-external-security-review-readback.ts` | `bun run readback:fg6:external-security-review`; `bun scripts/fg6-external-security-review-readback.ts inspect --evidence docs/verification/fg6-external-security-review-readback.json --json` |
| Representative benchmark | FG6-13 | Verified | `docs/verification/fg6-representative-benchmark-readback.json`; `docs/verification/fg6-representative-benchmark.md`; `scripts/fg6-representative-benchmark-readback.ts` | `bun run readback:fg6:representative-benchmark`; `bun scripts/fg6-representative-benchmark-readback.ts inspect --evidence docs/verification/fg6-representative-benchmark-readback.json --json` |
| SLO readback | FG6-14 | Verified | `docs/verification/fg6-slo-readback.json`; `docs/verification/fg6-slo.md`; `packages/cloud/control-plane/src/index.ts`; `packages/cloud/control-plane/test/control-plane.test.ts`; `scripts/fg6-slo-readback.ts` | `bun run readback:fg6:slo`; `bun scripts/fg6-slo-readback.ts inspect --evidence docs/verification/fg6-slo-readback.json --json` |
| Retention deletion drill | FG6-15 | Verified | `docs/verification/fg6-retention-deletion-readback.json`; `docs/verification/fg6-retention-deletion.md`; `docs/verification/fg5-retention-staging-readback.json`; `docs/verification/fg2-install-revoke-readback.json`; `scripts/fg6-retention-deletion-readback.ts` | `bun run readback:fg6:retention-deletion`; `bun scripts/fg6-retention-deletion-readback.ts inspect --evidence docs/verification/fg6-retention-deletion-readback.json --json` |
| Ops runbook drill | FG6-16 | Verified | `docs/runbooks/control-plane-incidents.md`; `docs/verification/fg6-ops-runbook-readback.json`; `docs/verification/fg6-ops-runbook.md`; `docs/verification/fg3-attestation-security-suite.json`; `docs/verification/fg4-runner-key-lifecycle-e2e.json`; `docs/verification/fg5-control-plane-incident-drill.json`; `docs/verification/fg6-chaos-fault-matrix-readback.json`; `scripts/fg6-ops-runbook-readback.ts` | `bun run readback:fg6:ops-runbook`; `bun scripts/fg6-ops-runbook-readback.ts inspect --evidence docs/verification/fg6-ops-runbook-readback.json --json` |
| Feature flag rollout gate | FG6-17 | Verified | `packages/contracts/src/github-governance.ts`; `packages/cloud/github-app/src/index.ts`; `packages/cloud/control-plane/src/index.ts`; `docs/verification/fg6-feature-flag-readback.json`; `docs/verification/fg6-feature-flag.md`; `scripts/fg6-feature-flag-readback.ts` | `bun run readback:fg6:feature-flags`; `bun scripts/fg6-feature-flag-readback.ts inspect --evidence docs/verification/fg6-feature-flag-readback.json --json` |
| Rollback compatibility drill | FG6-19 | Verified | `docs/verification/fg6-rollback-compat-readback.json`; `docs/verification/fg6-rollback-compat.md`; `docs/api/control-plane-openapi.yaml`; `docs/api/control-plane-compatibility-policy.md`; `packages/contracts/src/github-governance.ts`; `packages/cloud/control-plane/src/index.ts`; `packages/cloud/github-app/src/index.ts`; `packages/cloud/runner/src/index.ts`; `scripts/fg6-rollback-compat-readback.ts` | `bun run readback:fg6:rollback-compat`; `bun scripts/fg6-rollback-compat-readback.ts inspect --evidence docs/verification/fg6-rollback-compat-readback.json --json` |

## Exit Gate Closure

| Exit Gate | Status | Evidence basis |
|---|---|---|
| FG6-EG1 AC-01..AC-06 immutable evidence | Verified | FG6-02 through FG6-07 readback artifacts and this manifest |
| FG6-EG2 platform matrix | Verified | `docs/verification/fg6-platform-workflow-matrix-readback.json` and hosted IPC/runner evidence |
| FG6-EG3 Privacy Contract and DLP | Verified | `docs/verification/fg6-privacy-dlp-readback.json` with static, dynamic, database, log, and artifact coverage |
| FG6-EG4 adversarial interception | Verified | `docs/verification/fg6-adversarial-governance-matrix-readback.json` plus replay, stale-head, trust, fork, and key-revoke evidence |
| FG6-EG5 fault recovery and SLO | Verified | `docs/verification/fg6-chaos-fault-matrix-readback.json` and `docs/verification/fg6-slo-readback.json` |
| FG6-EG6 release security | Verified | `docs/verification/fg6-security-release-readback.json` and `docs/verification/fg6-external-security-review-readback.json` |
| FG6-EG7 performance budget | Verified | `docs/verification/fg6-representative-benchmark-readback.json` and `docs/verification/fg6-slo-readback.json` |
| FG6-EG8 rollback compatibility | Verified | `docs/verification/fg6-rollback-compat-readback.json` |
| FG6-EG9 design partner gray period | Blocked | Requires real rollout telemetry using the published `archctx@0.1.0`; current rollout intake is DRAFT only |
| FG6-EG10 human launch gate | Blocked | Requires signed launch decision after FG6-18 rollout readback |

## AC-01 Trace

The FG6-02 readback reruns the installed `archctx` local product against a temporary ordinary Git repository with GitHub, ArchContext Cloud, and common LLM provider environment variables removed by name only. The command path is `doctor -> mcp install -> init -> sync -> context -> prepare -> status -> checkpoint -> complete -> review`.

The evidence records local-only egress, disabled cloud content upload, disabled secure MCP tunnel by default, disabled third-party telemetry, local MCP stdio configuration, fresh checkpoint, deterministic local completion, and a passing local review with zero errors. It does not require `_ops/env`, GitHub App installation, Cloud token, provider credential, Wrangler deploy, or staging mutation.

## AC-02 Trace

The FG6-03 readback composes two immutable FG3 E3 artifacts. `docs/verification/fg3-developer-review-check-readback.json` proves a real staging GitHub CheckRun named `ArchContext / Developer Review` on `Ancienttwo/arch-context` PR #2, with conclusion `success`, output title `Developer-attested`, `clean-commit-worktree` execution provenance, Attestation digest marker, and allowlisted GitHub egress only. `docs/verification/fg3-developer-review-process-e2e.json` proves the local Developer Review execution path uses the Challenge head and Git tree, indexes a temporary worktree instead of the dirty source checkout, submits the review, and cleans the temporary worktree after success.

The FG6 wrapper records only sanitized IDs, SHAs, booleans, digest prefixes, egress categories, and source-inspection results. It rejects missing provenance markers, non-GitHub Check URLs, unexpected GitHub egress categories, source/diff endpoint markers, nonce leaks, signature leaks, keychain refs, provider tokens, and private-key material.

## AC-03 Trace

The FG6-04 readback composes the staging supersede readback and real temporary PR synchronize evidence. `docs/verification/fg3-real-pr-synchronize-e2e.json` proves a real PR moved through `opened -> synchronize`, the opened-head Check became `neutral`/`Superseded`, the synchronize-head Check was a distinct `ArchContext / Developer Review` Check bound to the new head, and the temporary PR/branch were cleaned up. `docs/verification/fg3-check-supersede-readback.json` separately proves old/new staging Check publication for the current staging PR, records that no unsupported `stale` conclusion was attempted, and keeps GitHub egress to allowlisted metadata/check categories.

The same evidence records Control Plane guards for old active Challenge supersede and stale submit rejection before nonce consumption. The FG6 wrapper rejects same-head evidence, old/new CheckRun reuse, missing supersede title/summary, new Check head mismatch, stale nonce consumption, unexpected GitHub egress, code-content endpoints, webhook secret markers, JWT/token markers, and private-key material.

## AC-04 Trace

The FG6-05 readback composes the GitHub-hosted Organization Runner execution readback, the required context/ruleset expected-source readback, and the deterministic no-provider conclusion readback. `docs/verification/fg4-github-hosted-runner-readback.json` proves a real GitHub Actions `pull_request` run completed successfully, produced a metadata-only Organization Attestation artifact with `llmProviderConfigured:false`, verified the Attestation locally, published `ArchContext / Organization Runner` as `success` with `Organization-attested`, and cleaned up the temporary PR/branch. `docs/verification/fg4-organization-runner-ruleset-readback.json` proves the required status check context is `ArchContext / Organization Runner` with the staging App integration ID, and a Developer Attestation cannot satisfy Organization requiredTrust. `docs/verification/fg4-deterministic-conclusion-readback.json` proves provider env keys are cleared, deterministic gate output is the Attestation conclusion source, advisory conclusion injection is rejected, and upload payloads contain no provider credential or advisory material.

The FG6 wrapper rejects provider-enabled runner artifacts, Developer Review context substitution, missing App integration ID, missing cleanup, non-Organization Attestation, failed Organization Runner Check, provider credential leakage, advisory leakage, token/private-key leakage, and code-content endpoint markers.

## AC-05 Trace

The FG6-06 readback composes the static Privacy Contract audit plus immutable DLP artifacts from FG3, FG4, and FG5. `bun run verify:privacy-contract` scans `packages/cloud` and `packages/contracts/src` for generic GitHub client usage, forbidden PR Files/Contents/Blob/Tree endpoints, non-allowlisted GitHub API paths, forbidden methods, and diff/patch media types. `docs/verification/fg3-cloud-dlp-readback.json` proves staging cloud DTOs, egress envelopes, and tail logs have zero bait, forbidden endpoint, or private-content hits.

`docs/verification/fg4-runner-dlp-readback.json` proves GitHub Actions artifacts, logs, cache lines, and Cloud DTO summaries retain zero code-content, bait, forbidden endpoint/media, or secret matches. `docs/verification/fg5-full-plane-dlp-readback.json` proves D1 schema privacy plus database, log, trace, queue, and error exports have zero code-content, bait value, forbidden key, forbidden endpoint/media, or secret matches. The FG6 wrapper records only counts, categories, table names, source inspection booleans, and assertions; it rejects static findings, unexpected GitHub egress categories, PR Files/Contents/Blob/Tree/Diff markers, Source/Diff/Filename/Symbol/Model/Finding body keys, storage scan hits, source inspection failures, and secret markers.

## AC-06 Trace

The FG6-07 readback composes Local Core no-provider evidence, the live GitHub-hosted Organization Runner evidence, the FG4 deterministic conclusion fixture, and the FG6 Organization Runner release readback. `docs/verification/fg6-local-no-cloud-readback.json` proves the local first-experience review completes with provider env names removed and no LLM provider required. `docs/verification/fg4-github-hosted-runner-readback.json` proves the official Review Action ran in GitHub Actions, produced an Organization Attestation, and published `ArchContext / Organization Runner` with `llmProviderConfigured:false`.

`docs/verification/fg4-deterministic-conclusion-readback.json` proves provider env keys are cleared, the deterministic gate uses the fixed no-provider model digest, the Attestation result comes from `deterministic-gate`, an injected Advisory carrying conclusion-shaped fields is rejected, the upload payload contains no Advisory or provider credential, and leak counters are zero. `docs/verification/fg6-organization-runner-no-llm-readback.json` ties the same no-provider deterministic proof to the release AC-04 path. The FG6 wrapper rejects source inspection failures, provider-enabled artifacts, wrong model digest, Attestation/result drift, Advisory influence, provider credential leakage, nonzero secret counters, and missing release assertions.

## FG6-09 Adversarial Governance Matrix Trace

The FG6-09 readback composes immutable FG2/FG4 E3 evidence into one release matrix. `docs/verification/fg2-staging-evidence.json` proves the staging GitHub App permission surface is `checks:write`, `metadata:read`, `pull_requests:read`, and `statuses:write`, with no `contents` permission and a documented status-permission ruleset decision. `docs/verification/fg4-public-fork-adversarial-readback.json` proves a real public fork PR from `aimpact-inc/arch-context`, dangerous `pull_request_target` canary run count `0`, no fork Challenge issuance, neutral `ArchContext / Organization Runner`, and PR/branch cleanup.

`docs/verification/fg2-install-revoke-readback.json` proves installation suspend/revoke stops new and existing installation token use, stops synthetic webhook processing before pull-head/check egress, preserves no secret/private content, and restores the installation after readback. `docs/verification/fg4-organization-runner-ruleset-readback.json` and `docs/verification/fg4-developer-cannot-satisfy-organization-readback.json` prove required status context is `ArchContext / Organization Runner` with the staging App integration ID and that Developer Review success cannot satisfy Organization requiredTrust. `docs/verification/fg4-runner-key-lifecycle-e2e.json` proves revoked Runner Keys reject preflight/submit immediately and do not consume nonce state. The FG6 wrapper rejects expanded GitHub App permissions, fork secret exposure, any dangerous workflow run, weak revoke evidence, Developer Review context substitution, source inspection failure, token/private-key/webhook-signature leakage, and revoked-key nonce consumption.

## FG6-10 Chaos/Fault Matrix Trace

The FG6-10 readback composes FG5 reliability evidence into one release chaos matrix. `docs/verification/fg5-check-failure-readback.json` proves a signed staging-only failure-injection route on `archcontext.repoharness.com` simulates GitHub Check update `503` failures, schedules two retry queue messages, reaches max attempts, dead-letters the delivery, manually replays it to `PENDING`, and enqueues the replay message with attempt/error reset. `docs/verification/fg5-control-plane-incident-drill.json` proves dashboard/runbook coverage for Webhook backlog, verify failure, Check DLQ, and GitHub API failure.

`docs/verification/fg5-control-plane-gate.md` records the DB transaction rollback contract: statement-level failures at `INSERT INTO check_deliveries` and `UPDATE review_challenges` leave the Challenge leased, keep `consumed_at` null, preserve lease fields, and leave zero Attestation/Check delivery rows. The same gate records queue-send-after-commit behavior, duplicate Webhook idempotency, clock-skew rejection, and current-head Check publication guards. `packages/cloud/control-plane/test/control-plane.test.ts` is scanned for the active `api-clock-skew-too-large`, body/rate limit, `CHALLENGE_SUPERSEDED`, `HEAD_SHA_MISMATCH`, `TRUST_LEVEL_MISMATCH`, and no-`stale` conclusion assertions. The FG6 wrapper rejects missing fault classes, weak retry/DLQ/replay proof, missing DB rollback contract, clock-skew guard drift, current-head duplicate Check regression, source inspection failure, secret markers, and code-content markers.

## FG6-11 Security Release Scan Trace

The FG6-11 readback runs `bun audit --json` against the current `bun.lock`, generates a CycloneDX-style SBOM from `bun pm ls --all`, scans release source roots (`actions`, `deploy`, `packages`, `scripts`) for high/critical SAST patterns, and scans release text roots for GitHub/OpenAI/Slack token and private-key markers. `_ops`, `.git`, `.wrangler`, `node_modules`, generated artifacts, and test fixtures are excluded from the secret scan so local operator state and synthetic fixture strings do not enter release evidence.

The readback writes `docs/security/scans/fg6-release-sbom.cdx.json`, `docs/security/reviews/fg6-release-security-scan.md`, records `staging.fg6-release-security-scan` in `docs/security/scans/manifest.json`, and writes `docs/verification/fg6-security-release-readback.json`. The verified artifact records zero dependency advisories, 37 SBOM components, 108 SAST-scanned files with zero findings, 375 secret-scanned files with zero findings, and staging security manifest readback `ok:true`. The FG6 wrapper rejects any Critical/High dependency advisory, empty SBOM, high/critical SAST finding, secret finding, missing `_ops` exclusion, nonzero manifest Critical/High count, or failed staging manifest readback.

## FG6-12 External Security Review Trace

The FG6-12 readback composes FG6 Privacy DLP, FG6 adversarial governance, FG6 security release scan, and FG3 Attestation replay evidence into an independent release security review. It first re-runs the source inspectors for `docs/verification/fg6-privacy-dlp-readback.json`, `docs/verification/fg6-adversarial-governance-matrix-readback.json`, `docs/verification/fg6-security-release-readback.json`, and `docs/verification/fg3-attestation-security-suite.json`.

The generated review covers API Allowlist, Key lifecycle, Replay, Fork safety, logs/artifacts, and release security scan. The verified artifact records static Privacy Contract pass with 18 scanned files, `contents` permission absent, zero unexpected GitHub egress categories, installation revoke stopping token/challenge/check, Runner and Device revoked keys rejecting without nonce consumption, replay rejection reason `CHALLENGE_ALREADY_CONSUMED`, fork Challenge issuance blocked, dangerous fork workflow runs `0`, log/artifact/storage/release secret surfaces clean, and zero Critical/High/Medium/Low review findings. It writes `docs/security/reviews/fg6-external-security-review.md` and `docs/verification/fg6-external-security-review-readback.json`.

## FG6-13 Representative Benchmark Trace

The FG6-13 readback uses `/Users/chris/Projects/aiphabee` as the representative clean repository input and writes only to a temporary clone under `/tmp`. The source repo was clean at head `6b4bef688a7e3e8aea45a9c4fd5a9f69322c1548` with 1073 tracked files; the benchmark clone matched the same head and had zero dirty status lines.

The measured path is local-only: `git clone --no-hardlinks`, `codegraph init`, `archctx doctor`, `archctx init`, `archctx sync`, `archctx prepare`, `archctx checkpoint`, `archctx complete`, and `archctx review`. The verified artifact records CodeGraph init 2410 ms, CodeGraph sync 410 ms, prepare 280 ms, complete 390 ms, review 380 ms, end-to-end measured latency 5910 ms, and peak RSS 244563968 bytes. The local review passed with zero errors, the checkpoint was fresh, and the initial CodeGraph indexing was below the PRD 300000 ms representative budget.

## FG6-14 SLO Readback Trace

The FG6-14 readback defines release SLOs for Challenge create p95, Attestation verify p95, successful Check delivery p95, and eligible required-check success rate. Challenge create inherits the PRD S-09 2000 ms normal webhook request budget, Check delivery inherits the PRD S-08 60000 ms Attestation-submit-to-Check-update budget, Verify uses a 2000 ms release budget derived from the FG5 verifier metric and S-08 end-to-end budget, and success rate targets at least 0.995 across eligible required checks.

The script runs an in-memory Control Plane probe that creates one Challenge through `createReviewChallengeApi`, records `challenge_create_latency_ms`, then publishes one successful current-head Check delivery through `publishCurrentCheckDeliverySuccess`, recording `check_delivery_lag_ms`. The verified artifact records Challenge create p95 1 ms, FG5 verifier fixture p95 500 ms, successful Check delivery p95 40000 ms, and required-check success rate 3/3 = 1. It also verifies source coverage for `challenge_age_ms`, `verify_latency_ms`, `check_delivery_lag_ms`, `check_delivery_retry_total`, and `reject_reason_total`, plus incident dashboard/runbook coverage for webhook, verify, queue, and GitHub API alerts.

## FG6-15 Retention Deletion Trace

The FG6-15 readback composes the remote D1 retention purge artifact, the installation revoke/restore artifact, and a focused in-memory account-delete probe. `docs/verification/fg5-retention-staging-readback.json` proves the staging D1 purge leaves expired Webhook, unfinished Challenge, Attestation, legacy audit, Check delivery, revoked Runner, repository scope, and rotation-window rows at 0 while preserving recent and terminal audit-min rows; ordinary Attestation DELETE is still rejected and `retention_purge_authorizations` returns to 0.

`docs/verification/fg2-install-revoke-readback.json` proves installation suspend causes new and existing installation-token probes to return 403, stops synthetic webhook processing before pull-head/check egress, preserves no secret or private content, and restores the installation afterward. The account-delete probe creates an account, Device key, revoked Device marker, and account-scoped notification provider, then calls `deleteAccount`; verified postconditions show account absent, devices after delete 0, revoked Device marker removed, and account-scoped notification provider removed.

## FG6-16 Ops Runbook Trace

The FG6-16 readback verifies `docs/runbooks/control-plane-incidents.md` has complete Signal, Triage, Remediation, and Verification sections for Device Key compromise, Runner Key compromise, GitHub outage, and queue backlog. Each section is matched against required operational terms such as `DEVICE_REVOKED`, `RUNNER_REVOKED`, `github-api-failure`, `retry/backoff`, `DEAD_LETTER`, `PENDING`, `webhook-backlog`, and `check-dlq`.

The evidence composes FG3 Device revoke behavior, FG4 Runner Key lifecycle behavior, FG5 incident drill coverage, and FG6 chaos/fault matrix evidence. The verified artifact records revoked Device and Runner keys rejecting without nonce consumption, Runner recovery through replacement key registration, GitHub API 503 injection reaching DLQ then replaying to `PENDING`, queue backlog and Check DLQ alerts, retry/replay queue messages, and zero private-content or secret marker hits.

## FG6-17 Feature Flag Trace

The FG6-17 readback verifies the release flags for `developerCheck`, `organizationCheck`, and `requiredTrust` through the shared governance contract, GitHub App state machine, and Control Plane APIs. `packages/contracts/src/github-governance.ts` owns the default-on rollout policy, disabled reasons, Check name mapping, requiredTrust mapping, and metadata digests.

The GitHub App probe proves `developerCheck:false` and `organizationCheck:false` create no CheckRun or Challenge side effects. When `requiredTrust:false` is configured on an organization-required repository, the webhook path records the disabled Organization decision and falls back to `ArchContext / Developer Review` instead of creating an Organization Runner Check.

The Control Plane probe proves disabled flags reject new Challenge creation and Check delivery queue side effects with `governance-feature-disabled:*` errors. The queue allow path still preserves the stable `ArchContext / Developer Review` context when the Developer Check flag is enabled, and publication paths reject disabled requiredTrust/check combinations before writing Check delivery side effects.

## FG6-19 Rollback Compatibility Trace

The FG6-19 readback verifies Schema, Check Context, and Action version rollback behavior without mutating staging. `scripts/fg6-rollback-compat-readback.ts` reads the contracts, Control Plane, GitHub App, Review Action, OpenAPI, compatibility policy, and workflow sources, then runs focused local probes.

The schema probe confirms current `ReviewChallenge` and `Attestation` schemas are `archcontext.review-challenge/v2` and `archcontext.attestation/v2`, and that old Challenge and Key API request schema versions are rejected with `challenge-api-schemaVersion-invalid` and `key-api-schemaVersion-invalid`. The rollback probe migrates a legacy Attestation v1 into `legacy-audit-only`, verifies `requiredCheckEligible:false`, submits it against a v2 required Check, and confirms rejection with `ATTESTATION_SCHEMA_UNSUPPORTED` without consuming nonce or moving the Challenge out of `LEASED`.

The Check Context probe verifies `ArchContext / Developer Review` and `ArchContext / Organization Runner` stay separated: each succeeds only with matching v2 provenance, Developer evidence cannot satisfy Organization Runner, Organization evidence cannot satisfy Developer Review, and Control Plane Check delivery rejects context mismatch with `TRUST_LEVEL_MISMATCH`. The Action probe verifies the current Review Action runtime version is accepted, an old runtime version is rejected with `runtime-version-mismatch`, the reusable workflow calls `archcontext/review-action@v1`, and caller templates stay pinned by commit SHA.
